cmake_minimum_required(VERSION 3.13)

set_property(GLOBAL PROPERTY USE_FOLDERS ON)
# External dependencies
# add_subdirectory(box2d)
add_subdirectory(lua)
add_subdirectory(imgui)
add_subdirectory(freetype)
# add_subdirectory(SPIRV-Cross)
add_subdirectory(spdlog)
add_subdirectory(ModelLoaders/meshoptimizer)
add_subdirectory(msdf-atlas-gen)
add_subdirectory(stb)
add_subdirectory(tinyply)
add_subdirectory(pugixml)
add_subdirectory(nanobind)
add_subdirectory(ozz-animation)
add_subdirectory(spz)
add_subdirectory(tinygsplat)
add_subdirectory(xatlas)
if(NOT IOS AND NOT ANDROID)
    add_subdirectory(glfw)
endif()
# if(APPLE)
#     add_subdirectory(MoltenVK/MoltenVKShaderConverter)
# endif()

# set_target_properties(box2d PROPERTIES FOLDER "external")
set_target_properties(lua PROPERTIES FOLDER "external")
set_target_properties(imgui PROPERTIES FOLDER "external")
set_target_properties(freetype PROPERTIES FOLDER "external")
set_target_properties(spdlog PROPERTIES FOLDER "external")
set_target_properties(meshoptimizer PROPERTIES FOLDER "external")
set_target_properties(msdf-atlas-gen PROPERTIES FOLDER "external")
set_target_properties(stbimage PROPERTIES FOLDER "external")
set_target_properties(tinyply PROPERTIES FOLDER "external")
set_target_properties(pugixml-static PROPERTIES FOLDER "external")
# set_target_properties(nanobind PROPERTIES FOLDER "external")
set_target_properties(ozz_animation PROPERTIES FOLDER "external")
set_target_properties(ozz_base PROPERTIES FOLDER "external")
set_target_properties(ozz_animation_offline PROPERTIES FOLDER "external")
if(NOT IOS AND NOT ANDROID)
    set_target_properties(glfw PROPERTIES FOLDER "external")
endif()
# if(APPLE)
#     set_target_properties(MoltenVKShaderConverter PROPERTIES FOLDER "external")
# endif()
set_target_properties(tinygsplat PROPERTIES FOLDER "external")
set_target_properties(spz PROPERTIES FOLDER "external")
set_target_properties(xatlas PROPERTIES FOLDER "external")

